import { RemoveFocusDirective } from './remove-focus.directive';

describe('RemoveFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveFocusDirective();
    expect(directive).toBeTruthy();
  });
});
